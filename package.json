{
  "name": "heatline-pursuit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "start": "vite preview --host 0.0.0.0",
    "test": "npm run build && node --test tests/vite-build.test.mjs",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "@types/three": "0.180.0",
    "typescript": "5.9.3",
    "vite": "8.0.13"
  }
}
